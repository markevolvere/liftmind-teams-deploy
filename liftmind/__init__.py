"""
LiftMind — RAG-powered lift technician assistant.

This package is the brain behind the LiftMind Teams bot.
It is interface-agnostic: brain.process_query() accepts any string user_id
and returns a plain string response, with no dependency on Telegram or Teams.
"""
